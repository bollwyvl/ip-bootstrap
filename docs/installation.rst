============
Installation
============

At the command line::

    $ easy_install ip-bootstrap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ip-bootstrap
    $ pip install ip-bootstrap